# 주식 대시보드 릴리즈 노트 (v1.0.0)

**배포 일자**: 2026-04-29

## 🚀 새로운 기능 (New Features)
- **주요 지표 대시보드**: 미국(S&P 500, NASDAQ, 다우 등), 국내(KOSPI, KOSDAQ), 원자재(WTI), 변동성 지표(VIX)의 현재가, 등락률, 그리고 일일 시세 흐름을 보여주는 **미니 라인 차트**를 구현했습니다.
- **주도 테마 실시간 스크래핑**: 네이버 금융 테마 시세를 크롤링하여, 당일 시장을 주도하는 상위 10개 테마와 그 주도 종목(관련주) 정보를 제공합니다.
- **캐싱을 통한 성능 최적화**: API 호출 및 웹 스크래핑 결과에 메모리 캐싱(`cachetools`)을 적용하여 빈번한 새로고침 시에도 지연 없는 화면 렌더링을 보장합니다.

## [1.1.0] - 2026-05-03 (Feature Update)

### 🚀 기능 (Features)
- **미국 주식 상승률 상위 종목 리스트**: 미국 주식시장(뉴욕, 나스닥)의 당일 상승률 상위 50개 종목 리스트를 제공합니다.
  - 나스닥/뉴욕 거래소를 탭으로 손쉽게 전환하여 조회할 수 있습니다.
  - 종목별 미니 차트와 함께 현재가, 거래량, 거래대금, 시가총액 정보를 한눈에 파악할 수 있습니다.
  - 현재가, 거래량, 시가총액 테이블 헤더 클릭 시 즉각적인 오름차순/내림차순 정렬 기능을 지원합니다.
  - 순위 리스트에서 종목을 클릭하면 상단의 트레이딩뷰 위젯에 해당 종목의 상세 차트가 자동으로 연동되어, 원활한 트레이딩 경험을 제공합니다.

## [1.0.0] - 2026-04-30 (Production Release)
- **전체 시장 개편**: 미국 시장과 한국 시장 탭을 나누어 독립된 레이아웃 제공.
- **섹터 및 테마 시황**: 핀비즈(Finviz) API를 연동하여 미국 주식 시장의 섹터 및 세부 테마 흐름 시각화 보드 추가.
  - 이름, 등락률 순 정렬 기능 지원.
  - 전체 보기 및 하위 3개 테마 요약 보기 토글 지원.
- **지표 마감 시간 보정**: yfinance 연동 시 한국 시간 기준 `14:55` 멈춤 현상을 감지하여 정규장 마감인 `15:30`으로 자동 렌더링되도록 버그 픽스.

### 🛠 배포 (Deployment)
- **운영 서버 배포 환경 구축**: 
  - Ubuntu 22.04 LTS (ARM64) 환경 배포 준비 완료.
  - Gunicorn과 Nginx 리버스 프록시 조합으로 80 포트를 통한 상용 서비스 환경 세팅 완료.
  - `deploy.sh` 원클릭 배포 자동화 스크립트 제공.
  - 배포 가이드 문서 (`docs/deploy_guide.md`) 추가.

---

## 🛠 실행 및 배포 방법 (How to Run)
1. Python 환경 (3.8 이상 권장) 준비
2. `requirements.txt` 패키지 설치: `pip install -r requirements.txt`
3. 구동 스크립트 실행: `./run.sh` 또는 `streamlit run src/main.py`

## 📝 알려진 제약 사항 (Known Issues / Limitations)
- **야간선물 데이터**: 무료 API로 조회하기 어려운 야간선물 데이터는 1.0.0 버전에서 제외되었으며, 향후 전용 API가 도입될 경우 활성화될 예정입니다.
- **미니 차트 시간 지연**: 무료 금융 API(`yfinance` 등)를 이용하므로, 시장 개장 직후 초기에는 실시간 분봉 데이터 반영에 약간의 시간 지연이 있을 수 있습니다.

---

## [1.2.2] - 2026-06-07 (Infrastructure & Reliability Update)

### 🚀 기능 및 배포 개선 (Infrastructure)
* **Nginx 및 WAS(Flask) Docker Compose 완전 통합**: 
  - 기존에 개별적으로 분리 운영되던 Nginx(호스트)와 WAS(도커)를 모두 독립 컨테이너화하여 Docker Compose 상의 가상 Bridge 네트워크에서 조립하여 통신하도록 일원화하였습니다.
  - 이로써 개발자의 로컬 테스트 환경과 상용 운영서버 환경의 차이를 0%로 극소화하였습니다.
* **배포 충돌 자동 방지**: 
  - `deploy.sh` 실행 시, 호스트 OS에서 80 포트를 점유하고 있던 Nginx 서비스 및 Gunicorn 백그라운드 서비스(`stock.service`)를 정지 및 비활성화하는 사전 작업을 자동 수행하여 기동 실패를 미연에 방지합니다.

### 🛠 버그 수정 및 최적화 (Bug Fixes)
* **Gunicorn 워커 부팅 타임아웃 방지**:
  - Gunicorn 기동 파라미터에 `--timeout 120`을 부여하여, 최초 데이터 캐싱 수집 지연 시 워커가 마스터 프로세스에 의해 킬되어 무한 재부팅 루프에 빠지던 핵심 장애 요인을 해결하였습니다.
* **SSE(Server-Sent Events) 끊김 완화**:
  - Nginx 리버스 프록시 단에 `proxy_read_timeout 86400s`를 적용하여, 실시간 지수/테마 갱신 스트림이 연결 대기 시간에 의해 주기적으로 탈락하는 현상을 보완하였습니다.
* **환경 변수 바인딩 경로 보완**:
  - Gunicorn이 `src/` 디렉토리로 체인지 디렉토리하여도 컨테이너 및 소스 내부에서 환경 변수(`.env`)를 차질 없이 호출할 수 있도록 Compose의 `env_file` 및 볼륨 볼 바인딩 설정을 견고히 보강하였습니다.

